{
  "$schema": "https://json.schemastore.org/nodemon.json",
  "verbose": true,
  "watch": ["./src/**"],
  "ext": "ts,js,json",
  "ignore": ["./src/**/*.spec.ts"],
  "exec": "ts-node --project ./configs/tsconfig.ts-node.json ./src/index.ts",
  "execMap": {
    "ts": "ts-node"
  }
}
